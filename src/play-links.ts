/**
 * Real, out-of-character destinations. The two closed-test links are still
 * open (PLAN.md §8): fill them in once the Play closed-test track is live and
 * /play switches from placeholders to buttons automatically.
 */
export const PLAY_LINKS = {
  web: 'https://superior-league-2036.vercel.app/',
  /** Google Group testers join first. */
  testerGroup: null as string | null,
  /** Play Console opt-in URL, opened after joining the group. */
  playOptIn: null as string | null,
};
